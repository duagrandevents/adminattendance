/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./**/*.js",
    ],
    theme: {
        extend: {
            colors: {
                // Custom colors if needed
            }
        },
    },
    plugins: [],
}
